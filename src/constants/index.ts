export enum BOX_STATUS {
  AVAILABLE = "AVAILABLE",
  SELECTED = "SELECTED",
  OCCUPIED = "OCCUPIED",
}

export enum BookingActionType {
  SELECT = "SELECT",
  RESET = "RESET",
  BLACK = "BLACK",
  WHITE = "WHITE",
}
